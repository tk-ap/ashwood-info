import test from 'node:test';import assert from 'node:assert/strict';import {normalizeMarket,loadMarkets} from '../paper-lab/kalshi.mjs';
test('normalize dollar strings and preserve missing quotes',()=>{const m=normalizeMarket({ticker:'KXBTC15M-TEST',status:'open',yes_ask_dollars:'0.9300',no_ask_dollars:'',close_time:'2026-09-23T21:00:00Z'});assert.equal(m.yesAsk,.93);assert.equal(m.noAsk,null)});
test('read-only public feed URL and sort',async()=>{let request;const fetcher=async(u,opts)=>{request={u,opts};return {ok:true,json:async()=>({markets:[{ticker:'LATER',status:'open',close_time:'2026-09-23T22:00:00Z'},{ticker:'SOONER',status:'open',close_time:'2026-09-23T21:00:00Z'}]})}};const x=await loadMarkets(fetcher);assert.deepEqual(x.map(m=>m.ticker),['SOONER','LATER']);assert.match(request.u,/series_ticker=KXBTC15M/);assert.equal(request.opts.cache,'no-store')});
test('fail closed on feed error',async()=>{await assert.rejects(()=>loadMarkets(async()=>({ok:false,status:429})),/unavailable/)});
