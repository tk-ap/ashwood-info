const KEY='ashwood.runway.v1';const seed=[{idea:'Submit more casting opportunities',lane:'Castings',horizon:'Now'},{idea:'Photograph strongest artwork and test a small print run using free print access',lane:'Art / prints',horizon:'Now'},{idea:'Apply to qualified Career Ops roles',lane:'Career',horizon:'Now'},{idea:'Identify a bounded finance / business freelance offer',lane:'Freelance / services',horizon:'Soon'},{idea:'Test product revenue only where there is demonstrated demand',lane:'Product revenue',horizon:'Later'}];let items=[];try{items=JSON.parse(localStorage.getItem(KEY)||'[]')}catch{}if(!items.length)items=seed;const feed=document.querySelector('#runway-feed'),form=document.querySelector('#runway-form'),idea=document.querySelector('#runway-idea'),lane=document.querySelector('#runway-lane'),horizon=document.querySelector('#runway-horizon');function save(){localStorage.setItem(KEY,JSON.stringify(items))}function render(){feed.innerHTML=items.map((x,i)=>`<article><div><span>${x.lane}</span><small>${x.horizon}</small></div><strong>${x.idea.replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}</strong><button type="button" data-done="${i}">Done</button></article>`).join('');feed.querySelectorAll('[data-done]').forEach(b=>b.onclick=()=>{items.splice(Number(b.dataset.done),1);save();render()})}form?.addEventListener('submit',e=>{e.preventDefault();if(!idea.value.trim())return;items.unshift({idea:idea.value.trim(),lane:lane.value,horizon:horizon.value});idea.value='';save();render()});document.querySelector('#runway-add-focus')?.addEventListener('click',()=>idea?.focus());save();render();